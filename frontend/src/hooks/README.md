// Add this import at the top of Layout.jsx after existing imports:
// import { useKeyboardShortcuts } from '../../hooks/useKeyboardShortcuts';
// Then call inside the component body:
// useKeyboardShortcuts();
//
// This file documents the addition — the hook is imported and called in Layout.jsx
// to activate Cmd+K → search navigation globally.
export default {};
